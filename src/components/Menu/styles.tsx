import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    userInfoSection: {
        flexDirection: "row",
    },
    avatarUser: {
        width: 75,
        height: 75,
        marginTop: 15,
    }
});

export default styles