import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        width: "100%",
        height: "auto",
        alignItems: "center",
    },
    logoImg: {
        width: 100,
        height: 100,
    },
});

export default styles